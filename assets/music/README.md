# Music

Replace `background-music.mp3` with your own song whenever you want.

Keep the same filename if you do not want to edit code.

Supported browser-friendly formats include `.mp3`, `.ogg`, and `.wav`. If you change the filename or extension, update the `<audio>` `src` in `index.html`.
